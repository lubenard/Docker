docker run -d -name Abathur -p 3000:3000 --mount type=bind,source=$HOME, target=/ python:2-slim
docker exec Abathur pip install flask
docker exec echo "from flask import Flask
app = Flask(__name__)

@app.route('/')
def hello_world():
    return '<h1>Hello, World!</h1>'

if __name__ == '__main__':
    app.run()" > hello_world.py
